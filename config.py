# This file holds all configuration settings for the project.
# It allows for easy modification of the tool's behavior without changing the core logic.

# A list of software packages that the tool will attempt to install.
# Note: The 'git' package is included as a core requirement for the version control system.
PACKAGES_TO_INSTALL = [
    "git",
    "vim",
    "curl",
    "python3-venv"
]